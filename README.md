# DSA-C-
// To make output there are 2 commands in c++
for minigw 
---> g++ filename.cpp
--->./a.exe  
//endl used for next line........
vector-->
are the dynamic arrays...
it can be resize when insert/delete elements..
contigous memory allocation
basic operators in vectors 
1.Declaration of vector
#include<vector>
int<datatype>vector-name;
vector<int>;
to know the size of the vector 
v.size()---> length
3.Resize
v.resize(viewsize);
v.size(8);
v.size()=8;
5.Capacity
V.capacity()
V.resize(8);

